export function Footer() {
  return (
    <footer className="border-t border-white/40 bg-white/80 backdrop-blur">
      <div className="mx-auto flex max-w-6xl flex-col items-start justify-center gap-2 px-4 text-xs text-muted sm:flex-row sm:items-center sm:justify-between">
        <span>Fastned Station Explorer (UK) — POC</span>
        <span>Data attribution required for Fastned UK Open Data.</span>
      </div>
    </footer>
  );
}
